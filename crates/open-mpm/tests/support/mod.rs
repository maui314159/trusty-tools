//! Shared test-support module. See `project.rs` for the integration test
//! `Project` object that spawns the open-mpm binary against a tempdir.

#![allow(dead_code)]

pub mod api_server;
pub mod project;
